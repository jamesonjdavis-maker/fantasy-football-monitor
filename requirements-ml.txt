# Optional ML/analytics dependencies. Only needed for the ffmonitor.ml module
# (historical usage modeling). The core monitor runs without these.
#   pip install -r requirements-ml.txt
nfl_data_py>=0.3.2
pandas>=2.0.0
# For the future breakout classifier (not needed for the baseline signal yet):
# scikit-learn>=1.3.0
# lightgbm>=4.0.0
